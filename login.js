const fs = require('fs');
const crypto = require('crypto');

class userManager {
constructor(path) {
    this.path = path;
}
create (userData) {
    const { firstName, lastName, email, age, password, cours} =userData
}
}
}